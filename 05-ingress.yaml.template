---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tautulli-ingress
  namespace: tautulli
  annotations:
    traefik.ingress.kubernetes.io/router.tls.certresolver: your-certresolver-name
spec:
  rules:
  - host: "tautulli.your-domain.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tautulli-service
            port:
              name: http
  tls:
  - hosts:
    - "tautulli.your-domain.com"
